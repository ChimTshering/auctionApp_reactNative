import { View, Text } from "react-native";
import { GlobalStyle } from "../constent/color";

export const WinnerList = () => {
  return (
    <View style = {GlobalStyle.RootScreenContainer}>
      <Text>List of Winners </Text>
    </View>
  );
};
